# 13 — 测试策略与质量门禁（引导版模板）

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-RA |
| 模块名称 | 投研助手（Research Assistant） |
| 文档版本 | v0.1 |
| 阶段 | Test（断言 — 怎样算通过） |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 4 层，写测试前先搞清楚每层验什么。

| 层级 | 工具 | 覆盖对象 | 说明 |
|------|------|----------|------|
| **L1 Unit** | pytest | 单个函数/类的逻辑 | 不依赖网络，最快 |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 存储 | 模拟 HTTP 请求 |
| **L3 Contract** | pytest + schema 断言 | 响应体结构与 `09` 一致 | 验字段名、类型 |
| **L4 E2E** | Playwright / Cypress | 完整用户旅程 | 浏览器级操作 |

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源 | 计数方法 | 示例 |
|------|----------|------|
| 正常路径 | 每个端点的 happy path = 1 条 | `POST /ask → 200` = TC-001 |
| 错误码 | 每个 distinct error code = 1 条 | `EMPTY_QUERY` = TC-002 |
| 分支路径 | 每个 if/else 降级分支 = 1 条 | 无 API Key → demo = TC-004 |
| 类型约束 | Spec `09` 约定的字段类型 = 1 条 | `llm_used` 必须 bool = TC-006 |

## 三、★ 示例：已有测试用例

| TC-ID | 层级 | 测试函数 | 断言要点 | 关联 REQ |
|-------|------|----------|----------|----------|
| TC-M01-001 | Integration | `test_ask_success` | `POST /ask` → 200, 含 answer、llm_used、traceId | REQ-001 |
| TC-M01-002 | Integration | `test_ask_empty_query` | 空 query → 400, `EMPTY_QUERY` | REQ-005 |
| TC-M01-003 | Integration | `test_ask_long_query` | query >500 → 400, `INVALID_QUERY` | REQ-005 |
| TC-M01-004 | Integration | `test_bailian_fallback` | 无 API Key → `answer_source='demo'` | REQ-003 |

### 3.1 会话管理 API 测试（请填写）

> 覆盖 GET /sessions、POST /sessions、DELETE /sessions/<id>。至少写 3~4 条。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-020 | Integration | `GET /chat/sessions` → 200, 返回 `items` 数组含 `session_id`、`title`、`report_ids`、`message_count`、`created_at` | AC-010-2 | P0 |
| TC-M01-021 | Integration | `POST /chat/sessions` → 201, 含 `session_id(string)`、`title(string)`、`report_ids(array)`、`created_at(ISO-8601)` | AC-003-2 | P0 |
| TC-M01-022 | Integration | `POST /chat/sessions` body 缺 `report_ids` → 400, `MISSING_FIELD` | AC-003-2 | P0 |
| TC-M01-023 | Integration | `DELETE /chat/sessions/{id}` 存在会话 → 200, 同步删除关联消息 | AC-010-4 | P0 |
| TC-M01-024 | Integration | `DELETE /chat/sessions/{id}` 不存在 → 404, `SESSION_NOT_FOUND` | AC-010-4 | P0 |
| TC-M01-025 | Integration | `GET /chat/sessions/{id}` 存在会话 → 200, 含 `session_id`、`title`、`report_ids`、`created_at` | AC-010-3 | P0 |
| TC-M01-026 | Contract | `POST /chat/sessions` 响应体字段类型：`session_id: string`, `report_ids: array`, `created_at: string(ISO-8601)` | AC-003-2 | P0 |

### 3.2 问答记录 API 测试（请填写）

> 覆盖 GET /sessions/<id>/records。有记录和无记录两种场景。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-030 | Integration | `POST /chat/sessions/{id}/messages` 正常发送 → 200, 含 `message_id`、`role: "assistant"`、`content: string`、`citations: array` | AC-003-1, AC-010-1 | P0 |
| TC-M01-031 | Integration | `GET /chat/sessions/{id}/messages` 无消息时 → 200, `messages: []`（空数组而非 404） | AC-010-1 | P1 |
| TC-M01-032 | Integration | `POST /chat/sessions/{id}/messages` 不存在 session → 404, `SESSION_NOT_FOUND` | AC-003-1 | P0 |
| TC-M01-033 | Contract | `POST /chat/sessions/{id}/messages` 响应体：`citations[].index: integer`, `citations[].page: integer`, `citations[].text: string` | AC-004-1, AC-004-4 | P0 |

### 3.3 Storage 层单元测试（请填写）

> 覆盖 Storage 类核心方法：`__init__`、`create_session`、`get_sessions`、`delete_session`（级联）、`add_record`、`get_records_by_session`。

| TC-ID | 层级 | 覆盖方法 | 断言要点 | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-040 | Unit | `Storage.__init__` | 目录不存在时自动创建；弹性初始化 JSON 文件 | P0 |
| TC-M01-041 | Unit | `create_session` | 返回 dict 含 `session_id(string)`、`title(string)`、`report_ids(list)`、`created_at(string ISO-8601)`、`query_count: 0` | P0 |
| TC-M01-042 | Unit | `get_sessions` | 无会话时返回 `[]`；创建后可查询到 | P0 |
| TC-M01-043 | Unit | `get_session_by_id` | 存在时返回完整 dict；不存在时返回 `None` | P0 |
| TC-M01-044 | Unit | `update_session` | `data` 内字段被写入；`updated_at` 自动更新为当前 UTC 时间；不存在 session 时抛 `KeyError` | P0 |
| TC-M01-045 | Unit | `delete_session` | 删除后 `get_session_by_id` 返回 `None`；关联 messages 同步清除 | P0 |
| TC-M01-051 | Unit | `add_message` | 返回 dict 含 `message_id(string)`、`role(string)`、`content(string)`、`citations(list)`、`created_at(string ISO-8601)` | P0 |
| TC-M01-052 | Unit | `get_messages_by_session` | 按 `session_id` 过滤返回；无消息返回 `[]` | P0 |
| TC-M01-053 | Unit | `delete_messages_by_session` | 删除后同一 `session_id` 的 `get_messages_by_session` 返回 `[]`；不影响其他 session 的消息 | P0 |

### 3.4 研报上传 API 测试

> 覆盖 POST /reports/upload、POST /reports/{id}/reparse。对应 AC-001-1 ~ AC-001-6。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-101 | E2E | 支持拖拽上传和点击选择两种方式上传 PDF | AC-001-1 | P0 |
| TC-M01-102 | Integration | 上传 PDF 文件（≤50MB）→ 201, 含 `report_id`、`status: pending` | AC-001-2 | P0 |
| TC-M01-103 | Integration | 上传 HTML 文件（≤50MB）→ 201；上传非 PDF/HTML → 415, `INVALID_FILE_TYPE` | AC-001-2 | P0 |
| TC-M01-104 | Integration | 批量上传 10 个文件 → 全部成功；上传 11 个文件 → 400, `FILE_LIMIT_EXCEEDED` | AC-001-3 | P0 |
| TC-M01-105 | Integration | 上传后 `status` 自动变为 `processing`；可通过 GET /reports/{id} 查看解析进度 | AC-001-4 | P0 |
| TC-M01-106 | Integration | 解析完成后 GET /reports/{id} 返回 `key_points[]`、`financial_forecast` 等提取信息 | AC-001-5 | P0 |
| TC-M01-107 | Integration | 解析失败时 `status: failed`，GET /reports/{id} 返回 `error_message` | AC-001-6 | P0 |
| TC-M01-108 | Integration | POST /reports/{id}/reparse → 200, `status` 重置为 `processing` | AC-001-6 | P0 |
| TC-M01-109 | Integration | 上传文件 > 50MB → 413, `FILE_TOO_LARGE` | AC-001-2 | P0 |

### 3.5 研报列表 API 测试

> 覆盖 GET /reports、GET /reports/{id}。对应 AC-002-1 ~ AC-002-6。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-111 | Integration | GET /reports → 200, `items[]` 含 `title`、`company_name`、`broker`、`publish_date`、`status` | AC-002-1 | P0 |
| TC-M01-112 | Integration | GET /reports?keyword=宁德时代 → 返回匹配公司名称/股票代码/券商名称的结果 | AC-002-2 | P0 |
| TC-M01-113 | Integration | GET /reports?sort=created_at 或 ?sort=publish_date，验证排序正确 | AC-002-3 | P0 |
| TC-M01-114 | Integration | GET /reports?status=completed → 仅返回 `status: completed` 的研报 | AC-002-4 | P0 |
| TC-M01-115 | Integration | GET /reports?page=2&per_page=20 → `pagination.total_pages` 正确计算 | AC-002-5 | P0 |
| TC-M01-116 | Integration | GET /reports/{id} → 200, 返回完整研报详情；不存在 → 404, `REPORT_NOT_FOUND` | AC-002-6 | P0 |

### 3.6 智能问答 API 测试

> 覆盖 POST /chat/sessions/{id}/messages。对应 AC-003-1 ~ AC-003-6。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-121 | Integration | 在研报阅读页 POST /chat/sessions/{id}/messages → 200, `role: assistant`, `content` 非空 | AC-003-1 | P0 |
| TC-M01-122 | Integration | POST /chat/sessions/{id}/messages 后 5 秒内返回 `content`（mock 场景） | AC-003-3 | P0 |
| TC-M01-123 | Integration | 答案内容基于研报内容，无 hallucination；答案包含 `citations[]` 引用来源 | AC-003-4 | P0 |
| TC-M01-124 | Integration | 多轮对话：第二次 POST 消息时上下文连贯，能引用前文内容 | AC-003-5 | P0 |
| TC-M01-125 | Contract | `response_time_ms` 字段类型为 `integer`，`llm_used` 字段类型为 `boolean` | AC-003-3 | P0 |

### 3.7 原文引用 API 测试

> 覆盖 citations 结构、GET /reports/{id}/file。对应 AC-004-1 ~ AC-004-5。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-131 | Integration | 答案中引用信息标注来源编号 `citations[].index` | AC-004-1 | P0 |
| TC-M01-132 | Integration | GET /reports/{id}/file 返回文件流，前端可跳转到对应页码位置 | AC-004-2 | P0 |
| TC-M01-133 | E2E | 前端点击引用编号，原文预览区高亮显示对应位置 | AC-004-3 | P0 |
| TC-M01-134 | Integration | `citations[]` 包含 `page: integer`、`text: string`（段落摘要） | AC-004-4 | P0 |
| TC-M01-135 | Integration | 无明确来源时答案中提示“基于综合分析”，`citations` 为空数组 | AC-004-5 | P0 |

### 3.8 多研报对比 API 测试

> 覆盖 POST /compare。对应 AC-005-1 ~ AC-005-6。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-141 | Integration | POST /compare 选择 2-5 篇研报 → 201, 返回对比结果 | AC-005-1 | P0 |
| TC-M01-142 | Integration | POST /compare 不同公司研报 → 422, `COMPARE_SAME_COMPANY_ONLY` | AC-005-2 | P0 |
| TC-M01-143 | Integration | 对比结果包含 `rating`、`target_price`、`key_points` 自动对齐 | AC-005-3 | P0 |
| TC-M01-144 | Integration | `upside` 字段：高于现价为正（绿色），低于为负（红色） | AC-005-4 | P0 |
| TC-M01-145 | Integration | 核心观点差异部分高亮显示（前端验证） | AC-005-5 | P0 |
| TC-M01-146 | Integration | POST /compare 后 60 秒内返回完整对比结果 | AC-005-6 | P0 |
| TC-M01-147 | Integration | POST /compare 仅 1 篇或 > 5 篇研报 → 400, `COMPARE_LIMIT_EXCEEDED` | AC-005-1 | P0 |

### 3.9 研报股票联动 API 测试

> 覆盖 GET /reports/{id}、GET /stock/{code}/snapshot。对应 AC-006-1 ~ AC-006-6。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-151 | Integration | GET /reports/{id} 返回 `stock_code`，前端右侧展示关联股票 | AC-006-1 | P0 |
| TC-M01-152 | Integration | GET /stock/{code}/snapshot → 200, 含 `name`、`code`、`price`、`change_percent` | AC-006-2 | P0 |
| TC-M01-153 | Integration | GET /stock/{code} → 200, `indicators` 含 `pe_ttm`、`pb`、`market_cap` | AC-006-3 | P0 |
| TC-M01-154 | Integration | GET /stock/{code} → 200, `related_reports.count` 展示相关研报数量 | AC-006-4 | P0 |
| TC-M01-155 | Integration | GET /stock/{code} → 200, 含相关研报列表，点击可进入研报 | AC-006-5 | P0 |
| TC-M01-156 | Integration | GET /stock/{code}/snapshot 响应时间 ≤ 2000ms | AC-006-6 | P0 |

### 3.10 股票独立查询 API 测试

> 覆盖 GET /stock/search、GET /stock/{code}。对应 AC-007-1 ~ AC-007-6。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-161 | Integration | GET /stock/search?keyword=宁德 → 返回匹配结果 | AC-007-1 | P0 |
| TC-M01-162 | Integration | GET /stock/search 输入时返回建议列表，含 `code`、`name`、`pinyin` | AC-007-2 | P0 |
| TC-M01-163 | Integration | 搜索结果点击进入 GET /stock/{code} → 200 | AC-007-3 | P0 |
| TC-M01-164 | Integration | GET /stock/{code} → 200, `price` 含完整行情（开高低收、成交量等） | AC-007-4 | P0 |
| TC-M01-165 | Integration | GET /stock/{code} → 200, `related_reports.items[]` 展示该股票所有研报 | AC-007-5 | P0 |
| TC-M01-166 | Integration | 从股票详情页点击研报 → GET /reports/{id} → 200 | AC-007-6 | P0 |

### 3.11 研报收藏 API 测试

> 覆盖 POST /favorites、GET /favorites、DELETE /favorites/{id}。对应 AC-008-1 ~ AC-008-5。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-171 | Integration | POST /favorites → 201, 含 `favorite_id`、`report_id` | AC-008-1 | P0 |
| TC-M01-172 | Integration | 重复收藏同一研报 → 422, `DUPLICATE_FAVORITE` | AC-008-1 | P0 |
| TC-M01-173 | Integration | GET /reports 返回 `is_favorite: true`（已收藏研报） | AC-008-2 | P0 |
| TC-M01-174 | Integration | GET /favorites → 200, 返回所有收藏研报列表 | AC-008-3 | P0 |
| TC-M01-175 | Integration | DELETE /favorites/{id} → 200, 取消收藏成功 | AC-008-4 | P0 |
| TC-M01-176 | Integration | 收藏数据持久化：重启后 GET /favorites 仍返回已收藏研报 | AC-008-5 | P0 |

### 3.12 研报标注 API 测试

> 覆盖 POST /annotations、GET /annotations、DELETE /annotations/{id}。对应 AC-009-1 ~ AC-009-5。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-181 | Integration | POST /annotations → 201, 含 `annotation_id`、`page`、`text`（高亮文本） | AC-009-1 | P0 |
| TC-M01-182 | Integration | POST /annotations 含 `note` 字段 → 201, 笔记保存成功 | AC-009-2 | P0 |
| TC-M01-183 | Integration | GET /annotations?report_id=xxx → 200, 返回该研报所有标注 | AC-009-3 | P0 |
| TC-M01-184 | Integration | 点击标注可跳转到原文对应页码位置（前端验证） | AC-009-4 | P0 |
| TC-M01-185 | Integration | DELETE /annotations/{id} → 200, 标注删除成功 | AC-009-5 | P0 |

### 3.13 历史问答 API 测试

> 覆盖 GET /chat/sessions、GET /chat/sessions/{id}、DELETE /chat/sessions/{id}。对应 AC-010-1 ~ AC-010-5。

| TC-ID | 层级 | 断言要点 | 关联 AC | 优先级 |
|-------|------|----------|---------|--------|
| TC-M01-191 | Integration | GET /chat/sessions/{id} → 200, 含 `messages[]` 展示当前会话历史 | AC-010-3 | P0 |
| TC-M01-192 | Integration | POST /chat/sessions 时 `report_ids` 正确保存，与会话关联 | AC-010-5 | P0 |

## 四、质量门禁

| Gate-ID | 条件 | 阻塞合并 |
|---------|------|----------|
| **G-LINT** | `black --check` + `flake8` 通过，0 errors, 0 warnings | **是** |
| **G-UNIT** | Unit 测试全绿，覆盖率 ≥ 80%，执行时间 < 30s | **是** |
| **G-INT** | Integration 测试全绿，覆盖率 ≥ 70%，执行时间 < 120s | **是** |
| **G-AC** | P0 验收标准 100% 覆盖（56/56 AC 有对应 TC） | **是** |
| **G-PERF** | API 响应 `response_time_ms < 5000`（P99 < 3000ms） | 否（warning） |

### 执行命令

```bash
# 后端测试
cd groups/Group-X/NN/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=. --cov-report=term-missing
```

## 五、TDD 适用场景

| 场景 | TDD 价值 | 建议 |
|------|----------|------|
| 简单 CRUD（列表、删除） | 低 | 写完补测试 |
| 参数校验 / 错误码 | 中 | 建议 TDD |
| 多级降级 / 状态机 | **高** | **强烈建议 TDD** |
| 金融计算 / 风险判定 | **极高** | **必须 TDD** |

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| AC 编号 | 对应 TC | API 端点 | 覆盖状态 |
|---------|---------|----------|----------|
| AC-001-1 | TC-M01-101 | POST /reports/upload | ✅ 已覆盖 |
| AC-001-2 | TC-M01-102, TC-M01-103 | POST /reports/upload | ✅ 已覆盖 |
| AC-001-3 | TC-M01-104 | POST /reports/upload | ✅ 已覆盖 |
| AC-001-4 | TC-M01-105 | POST /reports/upload | ✅ 已覆盖 |
| AC-001-5 | TC-M01-106 | GET /reports/{id} | ✅ 已覆盖 |
| AC-001-6 | TC-M01-107, TC-M01-108 | POST /reports/{id}/reparse | ✅ 已覆盖 |
| AC-002-1 | TC-M01-111 | GET /reports | ✅ 已覆盖 |
| AC-002-2 | TC-M01-112 | GET /reports?keyword=xxx | ✅ 已覆盖 |
| AC-002-3 | TC-M01-113 | GET /reports?sort=xxx | ✅ 已覆盖 |
| AC-002-4 | TC-M01-114 | GET /reports?status=xxx | ✅ 已覆盖 |
| AC-002-5 | TC-M01-115 | GET /reports (pagination) | ✅ 已覆盖 |
| AC-002-6 | TC-M01-116 | GET /reports/{id} | ✅ 已覆盖 |
| AC-003-1 | TC-M01-121 | POST /chat/sessions/{id}/messages | ✅ 已覆盖 |
| AC-003-2 | TC-M01-021, TC-M01-030 | POST /chat/sessions | ✅ 已覆盖 |
| AC-003-3 | TC-M01-122 | POST /chat/sessions/{id}/messages | ✅ 已覆盖 |
| AC-003-4 | TC-M01-123 | POST /chat/sessions/{id}/messages | ✅ 已覆盖 |
| AC-003-5 | TC-M01-124 | POST /chat/sessions/{id}/messages | ✅ 已覆盖 |
| AC-003-6 | TC-M01-002, TC-M01-003 | POST /chat/sessions/{id}/messages | ✅ 已覆盖 |
| AC-004-1 | TC-M01-131 | POST /chat/sessions/{id}/messages | ✅ 已覆盖 |
| AC-004-2 | TC-M01-132 | GET /reports/{id}/file | ✅ 已覆盖 |
| AC-004-3 | TC-M01-133 | GET /reports/{id}/file | ✅ 已覆盖 |
| AC-004-4 | TC-M01-134 | POST /chat/sessions/{id}/messages | ✅ 已覆盖 |
| AC-004-5 | TC-M01-135 | POST /chat/sessions/{id}/messages | ✅ 已覆盖 |
| AC-005-1 | TC-M01-141 | POST /compare | ✅ 已覆盖 |
| AC-005-2 | TC-M01-142 | POST /compare | ✅ 已覆盖 |
| AC-005-3 | TC-M01-143 | POST /compare | ✅ 已覆盖 |
| AC-005-4 | TC-M01-144 | POST /compare | ✅ 已覆盖 |
| AC-005-5 | TC-M01-145 | POST /compare | ✅ 已覆盖 |
| AC-005-6 | TC-M01-146 | POST /compare | ✅ 已覆盖 |
| AC-006-1 | TC-M01-151 | GET /reports/{id} | ✅ 已覆盖 |
| AC-006-2 | TC-M01-152 | GET /stock/{code}/snapshot | ✅ 已覆盖 |
| AC-006-3 | TC-M01-153 | GET /stock/{code} | ✅ 已覆盖 |
| AC-006-4 | TC-M01-154 | GET /stock/{code} | ✅ 已覆盖 |
| AC-006-5 | TC-M01-155 | GET /stock/{code} | ✅ 已覆盖 |
| AC-006-6 | TC-M01-156 | GET /stock/{code}/snapshot | ✅ 已覆盖 |
| AC-007-1 | TC-M01-161 | GET /stock/search | ✅ 已覆盖 |
| AC-007-2 | TC-M01-162 | GET /stock/search | ✅ 已覆盖 |
| AC-007-3 | TC-M01-163 | GET /stock/search | ✅ 已覆盖 |
| AC-007-4 | TC-M01-164 | GET /stock/{code} | ✅ 已覆盖 |
| AC-007-5 | TC-M01-165 | GET /stock/{code} | ✅ 已覆盖 |
| AC-007-6 | TC-M01-166 | GET /stock/{code} | ✅ 已覆盖 |
| AC-008-1 | TC-M01-171, TC-M01-172 | POST /favorites | ✅ 已覆盖 |
| AC-008-2 | TC-M01-173 | GET /favorites | ✅ 已覆盖 |
| AC-008-3 | TC-M01-174 | GET /favorites | ✅ 已覆盖 |
| AC-008-4 | TC-M01-175 | DELETE /favorites/{id} | ✅ 已覆盖 |
| AC-008-5 | TC-M01-176 | POST /favorites, GET /favorites | ✅ 已覆盖 |
| AC-009-1 | TC-M01-181 | POST /annotations | ✅ 已覆盖 |
| AC-009-2 | TC-M01-182 | POST /annotations | ✅ 已覆盖 |
| AC-009-3 | TC-M01-183 | GET /annotations | ✅ 已覆盖 |
| AC-009-4 | TC-M01-184 | GET /annotations | ✅ 已覆盖 |
| AC-009-5 | TC-M01-185 | DELETE /annotations/{id} | ✅ 已覆盖 |
| AC-010-1 | TC-M01-030, TC-M01-031 | GET /chat/sessions/{id}/messages | ✅ 已覆盖 |
| AC-010-2 | TC-M01-020 | GET /chat/sessions | ✅ 已覆盖 |
| AC-010-3 | TC-M01-191 | GET /chat/sessions/{id} | ✅ 已覆盖 |
| AC-010-4 | TC-M01-023, TC-M01-024 | DELETE /chat/sessions/{id} | ✅ 已覆盖 |
| AC-010-5 | TC-M01-192 | POST /chat/sessions | ✅ 已覆盖 |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2025-04-14 | 首版填写 |
